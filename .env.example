# Mode: 'local' for local filesystem or 'sharepoint' for SharePoint (default: local)
MODE=local

# Local Mode Configuration
# BASE_PATH: Directory to scan for folders (defaults to current working directory if not specified)
# Leave empty or comment out to use current working directory
# BASE_PATH=/path/to/your/folders

# Output file name
OUTPUT_FILE=aggregated_questionnaires.xlsx

# SharePoint Mode Configuration (only needed if MODE=sharepoint)
# SHAREPOINT_SITE_URL=https://yourcompany.sharepoint.com/sites/yoursite
# SHAREPOINT_USERNAME=your.email@company.com
# SHAREPOINT_PASSWORD=your_password
# QUESTIONNAIRE_FOLDER_PATH=Shared Documents/questionnaire
